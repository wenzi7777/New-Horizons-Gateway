# New Horizons Gateway v0.1.4

- Restores the intended UDP-only board control path instead of falling back to a stale TCP-style shortcut.
- Updates the Gateway command routing so WebUI and upstream commands reuse the current UDP control session established by heartbeat and stream traffic.
- Keeps the existing Gateway WebUI update flow unchanged while aligning the runtime transport semantics with New Horizons OS and Desktop.
