# New Horizons Gateway v0.1.1

- Adds first-run auto-suggested Gateway ID instead of falling back to `local-gateway`.
- Keeps setup wizard editable so users can regenerate or override the suggested ID.
- Aligns Gateway release metadata and downloadable artifact with the standalone mirror.
