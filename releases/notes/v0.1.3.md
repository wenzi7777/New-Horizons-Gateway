# New Horizons Gateway v0.1.3

- Adds a Windows PowerShell deployment script for starting the Gateway on a local Windows host.
- Updates the Gateway WebUI target server preview so `Production` now immediately shows the `isensing-s1` WSS endpoint.
- Keeps the saved configuration and runtime restart flow unchanged.
