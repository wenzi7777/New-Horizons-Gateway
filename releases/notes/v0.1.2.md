# New Horizons Gateway v0.1.2

- Removes Gateway token persistence and token controls from the local WebUI.
- Moves `Update` to the end of the page and separates `Refresh now` from nearby-device discovery.
- Keeps local settings focused on Gateway ID, enable state, and target server selection.
