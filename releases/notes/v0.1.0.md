# New Horizons Gateway v0.1.0

- Adds first-run Gateway ID setup wizard.
- Defaults Gateway runtime to disabled until a valid ID is saved.
- Adds Gateway WebUI controls for ID, enabled state, and update operations.
- Adds update manifest, source zip download, SHA-256 verification, and apply/restart hooks.
- Keeps Docker deployments on manual update by default.
